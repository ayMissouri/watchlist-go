## Structure
`main` is for bootstrapping.

`server` is for router setup.

`handlers` is for request/response logic.

## Info

### go mod init
This creates a `go.mod` file which is the Go equivalent of starting a package-managed project and defining its module path. (`package.json`)

### golangci-lint
golangci-lint is the standard multi-linter most Go repos use
